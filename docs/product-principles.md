# 产品原则

思想孵化器把用户放在思想的提出者、解释者和最终判断者的位置。用户亲自说出的内容、AI 的理解、AI 的候选表达、用户接受或拒绝的表达必须保持可区分。

- 只有用户点击“准确表达了我”，候选节点才会成为用户接受的观点。
- 默认一次只让一个认知功能进行一次简短介入，然后等待用户。
- AI 输出优先是镜像、区分或问题，不自动生成最终答案。
- 多视角结果保持候选状态，不投票、不强制共识。
- 聊天事件推动状态变化，但事件不等同于思想本身。

这些原则在 src/domain/protocol.ts 的分类、介入选择和确认状态转换中实现。
